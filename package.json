{
  "name": "web-analyticsmanager",
  "version": "1.0.0",
  "description": "Library used to manage sending event data to various analytics tools, such as GA4, Facebook Pixel, etc.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "npm run format && tsc",
    "build:dev": "npm run format && tsc --watch",
    "test": "jest --coverage",
    "test:dev": "jest",
    "format": "prettier ."
  },
  "files": [
    "/dist/**/*.js",
    "/dist/**/*.d.ts"
  ],
  "keywords": [],
  "author": "Basic Invite, LLC",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^28.1.8",
    "jest": ">=28.0.0 <29.0.0-0",
    "prettier": "2.6.2",
    "ts-jest": "^28.0.8",
    "typescript": "^4.7.4"
  }
}