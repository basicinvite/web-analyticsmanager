{
  "name": "@web-analyticsmanager/main",
  "version": "1.0.0",
  "description": "Library used to manage sending event data to various analytics tools, such as GA4, Facebook Pixel, etc.",
  "main": "index.js",
  "scripts": {
    "build": "npm run format && tsc",
    "build:dev": "npm run format && tsc --watch",
    "test": "jest --coverage",
    "test:dev": "jest",
    "format": "prettier ."
  },
  "files": [
    "/dist/**/*.js"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^28.1.8",
    "jest": "^29.0.0",
    "prettier": "2.6.2",
    "ts-jest": "^28.0.8",
    "typescript": "^4.7.4"
  }
}